var slider = tns({
  container: ".try__slider",
  items: 3,
  speed: 200,
  gutter: 100,
  sliderBy: "page",
  autoplay: true,
  // navPosition: "bottom",
  // containerControls: "#controls",
  // prevButton: ".prev",
  // nextButton: ".next",
});
